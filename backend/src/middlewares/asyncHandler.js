/**
 * DEPRECATED: This file is a duplicate of `src/utils/asyncHandler.js`.
 * It is kept for backward compatibility in case of external imports, but
 * you should import from `../utils/asyncHandler.js` instead.
 * 
 * SOLID: Removing duplication (DRY) to ensure single source of truth.
 */
export { asyncHandler } from '../utils/asyncHandler.js';
